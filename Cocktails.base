filters:
  and:
    - file.folder == "Cocktails"
    - type == "cocktail"
formulas:
  Cocktail: link(file.name, file.name)
  AllInStock: ingredients.map(file(value).properties.in_stock).reduce(acc && value, true)
properties:
  formula.Cocktail:
    displayName: Cocktail
  note.name:
    displayName: Name
  note.description:
    displayName: Description
  note.ingredients:
    displayName: Ingredients
  note.image:
    displayName: Image
  formula.AllInStock:
    displayName: Available
views:
  - type: cards
    name: Available
    filters:
      and:
        - formula.AllInStock == true
    sort:
      - property: formula.Cocktail
        direction: ASC
    image: note.image
    imageAspectRatio: 1
    order:
      - formula.Cocktail
      - description
  - type: cards
    name: All
    sort:
      - property: formula.Cocktail
        direction: ASC
    image: note.image
    imageAspectRatio: 1
    order:
      - formula.Cocktail
      - description
